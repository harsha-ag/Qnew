<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.qnew">
    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Qnew">
        <activity android:name="com.example.qnew.search.searchlists" />
        <activity
            android:name=".NavigateHome"
            android:label="@string/title_activity_navigate_home"
            android:theme="@style/Theme.Qnew.NoActionBar">

        </activity>




        <activity android:name=".feedpost.post">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".search.searchlists" />
        <activity android:name=".feedpost.post"/>



    </application>

</manifest>